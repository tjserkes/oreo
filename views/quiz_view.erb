<h1>How do you eat an Oreo?</h1>
<div class="container">
	<div>
		<ul>
			<% @methods.each do |method| %>
				<li class="btn btn-block btn-lg" href="/quiz/<%= (method[:number]).to_i %>">
					<%= method[:description] %>
				</li>
			<% end %>
		</ul>
	</div>
</div>